services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: vaaniverse
      POSTGRES_PASSWORD: vaaniverse_dev
      POSTGRES_DB: vaaniverse
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
